package com.example.carappointmentservice;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class CarAppointmentServiceApplication {

    // Starts the Car Appointment Service Spring Boot application
    public static void main(String[] args) {
        SpringApplication.run(CarAppointmentServiceApplication.class, args);
    }
}
